{
  "name": "coffee",                // Name of your project
  "version": "1.0.0",              // Version of your project
  "description": "A sample Node.js application", // Description of your project
  "main": "index.js",              // Main entry point of your application
  "scripts": {
    "start": "node index.js",     // Command to start your application
    "build": "npm run build",     // Build command (can be customized)
    "test": "jest",               // Command to run tests using Jest
    "lint": "eslint .",           // Command to lint your code (optional)
    "prebuild": "echo 'Pre-build step'" // Optional step before the build
  },
  "author": "mendozareynaliza",           // Author of the project
  "license": "MIT",                // License of the project
  "dependencies": {
    "express": "^4.17.1"          // Example runtime dependency
  },
  "devDependencies": {
    "jest": "^29.0.0",            // Example development dependency for testing
    "eslint": "^8.0.0",           // Example development dependency for linting
    "webpack": "^5.0.0"           // Example build tool
  }
}

